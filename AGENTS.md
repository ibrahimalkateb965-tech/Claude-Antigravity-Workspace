<!-- markdownlint-disable MD033 MD041 -->
<div dir="rtl">

# دليل القواعد العام وتوجيهات التطوير الاحترافية للمشروع (AGENTS.md)

بصفتك المساعد البرمجي الخبير والمطور الرئيسي لهذا المشروع، يجب عليك الالتزام التام بالقواعد الفنية واللغوية التالية لضمان إنتاج كود برمجي ذي جودة عالية ترتقي إلى مستوى المشاريع العالمية (Production-Grade).

> [!CAUTION]
> **تم دمج قواعد التدقيق المعماري الصارم.** يمنع تماماً استخدام الترقيعات السطحية. القواعد هنا مصممة لضمان استقلالية، وموثوقية، وأمان نظام الوكلاء.

---

## 1. التوجيهات اللغوية وتنسيق الواجهة (Linguistic & RTL Constraints)

* **الكتابة باللغة العربية**: يجب أن تكون جميع الردود والتوضيحات باللغة العربية الفصحى السليمة (باستثناء مسارات الملفات، الأكواد البرمجية، أو أوامر الطرفية).
* **تنسيق RTL (من اليمين إلى اليسار)**: يجب تغليف ردودك ومستندات المتابعة (مثل `walkthrough.md`, `implementation_plan.md`, `learning_proposal.md`) دائماً بـ `<div dir="rtl">` لضمان عرضها بشكل سليم ومقروء للمستخدم العربي في مساحات عرض المحرر (Editor Areas).
* **المصطلحات البرمجية**: عند استخدام مصطلحات تقنية باللغة الإنجليزية وسط نص عربي، تأكد من عدم تشتيت ترتيب الجملة أو علامات الترقيم.
* **أسلوب التواصل المباشر**: الابتعاد التام عن أي مجاملات أو ترحيبات زائدة أو مضللة، والتركيز الحصري على تقديم الحلول الفنية المباشرة، الأرقام، وأفضل الممارسات البرمجية.

---

## 2. بنية وهندسة البرمجيات (Architecture & Clean Code)

بناءً على مبادئ **Code Architect** و **Backend Architect**:

* **بنية واضحة (Clean Architecture)**: إدارة قواعد البيانات والشبكات والتخزين بشكل مستقل تماماً عن واجهة المستخدم عبر طبقة الـ Domain، واستخدام `StateFlow` / Providers للقراءة فقط في واجهات العرض.
* **تفكيك الواجهات (UI Modularity)**: يُمنع كتابة ملفات واجهة مستخدم ضخمة. يجب تفكيك الشاشات المعقدة وتمرير الأحداث عبر (State Hoisting).
* **حقن الاعتماديات (Dependency Injection)**: الاعتماد على أنماط الـ DI أو المزودات القياسية وتجنب الإنشاء اليدوي العشوائي للكائنات الحيوية.

---

## 3. معايير جودة الكود ومراجعته (Code Review & Quality Standards)

بناءً على مبادئ **Code Reviewer**:

* **معالجة الأخطاء (Error Handling)**: معالجة جميع الاستثناءات بشكل صريح مع توفير واجهات توضح الخطأ للمستخدم وآلية لإعادة المحاولة (Retry Mechanism).
* **فلسفة كتابة الكود**: الأولويات: **قابلية الاختبار ← مقروئية الكود ← الاتساق ← البساطة ← القابلية للتراجع**. وتجنب الكود المكرر (DRY).

---

## 4. تحسين الأداء وإدارة الموارد (Performance Optimization)

بناءً على مبادئ **Performance Optimizer**:

* **كفاءة الذاكرة والأداء في واجهات المستخدم**: منع إعادة الرسم غير الضرورية وإدارة الموارد بحكمة.
* **العمل في الخلفية**: تنفيذ العمليات الطويلة ومزامنة البيانات عبر محركات المهام الخلفية لضمان استمراريتها بشكل آمن وموفر للبطارية.

---

## 5. استراتيجية الاختبار والتحقق التلقائي (Testing & QA Automation)

بناءً على مبادئ **Test Automator**:

* **احتكار الاختبارات لـ Claude Code**: في منظومة العمل وأسطول الـ CLIs، يُحظر تماماً على أي وكيل تنفيذي (`OpenCode`, `Antigravity`, إلخ) تشغيل الاختبارات أو اعتماد الكود. يتولى `Claude Code CLI` (Opus Max) حصرياً تشغيل كافة الاختبارات واعتماد النتائج.
* **هرم الاختبار (Test Pyramid)**: التركيز الأكبر على اختبارات الوحدة (Unit Tests)، ثم اختبارات التكامل (Integration)، ثم اختبارات الواجهة (UI/E2E).
* **إدارة بيانات الاختبار**: استخدام تقنيات المحاكاة (Mocking) لفصل الاعتماديات أثناء الفحوصات التلقائية وتجنب الاتصال الفعلي بالخوادم أثناء الاختبار.

---

## 6. منهجية استكشاف الأخطاء وإصلاحها (Systematic Debugging)

بناءً على مبادئ **Debugger**:

* اتباع السلوك المنهجي: 1. إعادة الإنتاج (Reproduce) -> 2. العزل (Isolate) -> 3. التحليل (Analyze) -> 4. الفرضية والاختبار (Hypothesize & Test) -> 5. التثبيت والتأكيد (Fix & Verify).

---

## 7. منهجية خط الإنتاج متعدد الوكلاء (Multi-Agent Pipeline Workflow)

يُلزم العميل الرئيسي باستدعاء الوكلاء وفقاً للملفات الموجودة في مجلد `.agents/Sub_Agent` واتباع التدفق التفاعلي التالي:

* **التحقق الأولي وتوفر التخصص (Crew Gap Check)**: تفعيل `[github-talent-scout]` للبحث وجلب الحلول إذا لزم الأمر.
* **الخطوات**: التصميم الهيكلي ← الكتابة البرمجية ← المراجعة وضمان الجودة ← الفحص والأتمتة.
* **التسجيل في الـ Logs**: يلتزم العميل الرئيسي في نهاية كل دورة عمل بتلخيص أسماء الـ Sub-agents الذين ساهموا في التعديل وأدوارهم.

---

## 8. الهيكلة وإدارة الذاكرة (Memory Management & Optimization Log)

* **المعمارية اللامركزية للذاكرة**: يجب أن يحتوي كل مشروع على مجلد `.agents/` خاص به بداخله `MEMORY_STORE.md` حصرياً لتسجيل الدروس المستفادة والاكتشافات، لمنع تضخم حجم ملف القواعد `AGENTS.md`.
* **الخطاف الحارس (Watchdog Hook) [CRITICAL FIX]**: لمنع ظاهرة "المشاريع فارغة الذاكرة"، يجب إعداد خطاف حارس يجبر النظام على أخذ لقطة لملف الـ Diff وتوثيق المنجزات تلقائياً في `MEMORY_STORE.md` قبل إغلاق أي مشروع، دون انتظار طلب مباشر من المستخدم.
* **الاعتمادية الخلفية (Legacy Support)**: عند دمج النظام في مشروع قديم يمتلك نظام وكلاء سابق، تضاف ملفات الذاكرة فقط ولا تُستبدل القواعد القديمة.

---

## 9. إدارة المهارات ونظام الخطافات (Skills & System Hooks)

* **الالتزام بالخطافات**: بمجرد قيام المستخدم بكتابة محفز (مثل "تم بنجاح"، "حدث خطأ"، "ابدأ ميزة")، يتم تفعيل الخطاف والوكيل المخصص فوراً وفقاً لملف `HOOKS_GUIDE.md`.
* **هيكل المهارات الموحد**: يجب أن يمتلك أي ملف مهارة `SKILL.md` ترويسة (YAML Frontmatter) تتضمن `name` و `description`.
* **قاعدة فصل المحفزات (Triggers Separation)**: عزل المحفز النصي عن وصف المحفز لضمان سهولة النسخ واللصق لتفعيل الوكلاء.

---

## 10. التكامل الخارجي وأدوات الذكاء الاصطناعي (AI & MCP Tools Rule)

* **أمان الأوامر الطرفية (Sandboxing & Absolute Paths) [CRITICAL FIX]**: يُمنع التشغيل العشوائي للأوامر الطرفية. **يُسمح** بتشغيل الأوامر الطرفية والسكربتات الخارجية *فقط* ضمن بيئة معزولة (Sandboxed) وبشرط الحصول على إذن صريح ومباشر من المستخدم للمصادقة على الأمان. ويجب دائماً تقديم الأوامر بالمسار المطلق الكامل `& "<Full_Path>"` وتجنب المسارات النسبية العارية لمنع أخطاء مسار عمل الطرفية (CWD).
* **أدوات MCP ومعايير الخوادم**: يُفضل الاعتماد على أدوات `Model Context Protocol`. وعند بناء أدوات جديدة عبر MCP, يجب تصدير الحزم بصيغة جاهزة للاستخدام عبر سيرفرات `node` أو `python`. **ويُحظر تماماً** تعديل أو إعادة تغليف `sys.stdout` في خوادم نمط `stdio` للحفاظ على كائن `buffer` سليم لقنوات JSON-RPC، مع توجيه كافة رسائل السجلات والتصحيح إلى `sys.stderr` حصراً.
* **أتمتة الاختبارات**: يتم اعتماد سكريبتات الأتمتة الموثوقة (مثل Maestro أو Flutter Driver) لفحص الواجهات وسيناريوهات الوصولية.

---

## 11. استراتيجية النماذج وتوزيع التكلفة (Model Cost Routing)

* **تحسين نافذة السياق (Context Window Optimization) [CRITICAL FIX]**: التوزيع الذكي للنماذج لا يكفي للحد من التكلفة. **يجب** إلزام وكيل `code-reviewer-quality` وأي وكيل بحث بمسح الكود برمجياً بنظام المجزآت (Chunking) وقراءة العناوين فقط (Signatures) بواسطة أدوات `lean-ctx`، وعدم التهام ملفات ضخمة بالكامل في نافذة السياق لمنع استنزاف الميزانية وانهيار تركيز النماذج.

---

## 12. الالتزام الصارم بالسياق العالمي وبروتوكول "الخطوة صفر" (Global Context Injection & Step Zero Protocol)

* **قاعدة حقن السياق (Step Zero Protocol):** يُمنع منعاً باتاً الاعتماد على الذاكرة المؤقتة أو البدء في أي مهمة برمجية قبل قراءة ملف القيود التقنية العالمي `ACTIVE_CONTEXT_INJECTION.md` الموجود في مسار الإعدادات (config).
* **التأكيد الداخلي الصامت (Silent Internal Citation):** كدليل قطعي على قراءتك الفعليّة للملف، **يجب أن يكون أول إجراء برمجي (Tool Action) لك هو استدعاء أداة قراءة الملف**، ويجب أن تقوم باقتباس القيود المتعلقة بالمهمة حرفياً **داخل تفكيرك الداخلي (Thought Process)** فقط. يُمنع طباعة نصوص القيود والاقتباسات الطويلة في واجهة المحادثة مع المستخدم، ويكفي الإشارة بجملة واحدة شاردة وخفيفة جداً (مثل: *"تم استدعاء بروتوكول الخطوة صفر وتطبيق القيود صامتاً"*). أي خطة أو كود يُولد دون قراءة الملف أولاً في التفكير الداخلي يُعتبر انتهاكاً جسيماً للبروتوكول.

---

## 13. بروتوكول تحول نمط المحادثة لقيادة الأسطول (Multi-CLI Fleet Session Mode - Hook 22)

* **التحول المستدام للأسطول**: بمجرد تفعيل الخطاف 22 (بالمحفزات "أسطول الـ CLIs", "تشغيل الأسطول", "تنسيق الأسطول", "fleet orchestrator", "multi cli fleet" أو ذكر خطاف 22 صراحة)، تتحول كافة ردود المساعد وأوامره في باقي المحادثة تلقائياً وبشكل مستمر إلى صيغة أوامر وتفويضات موجهة وموزعة لأفراد الأسطول باللغة الإنجليزية طبقاً للهيكل المعياري المتفق عليه (`Template 02: Task Delegation Protocol` و `Template 03: Code Review Audit`).
* **احتكار الفحص والاختبار لـ Claude Code فقط (Exclusive Testing & Verification Monopoly) [CRITICAL RULE]**:
  * **يُمنع منعاً باتاً على أي وكيل آخر (`OpenCode CLI` أو `Antigravity` أو أي Sub-agent) تشغيل أو تنفيذ أي اختبارات برمجية.**
  * **`Claude Code CLI` (النموذج: `Claude 5 Opus / Opus Max`) هو الجهة الوحيدة والمخوّلة حصرياً بـ:**
    1. تنفيذ وتشغيل كافة الاختبارات التلقائية (`flutter test`, `unit tests`, `integration tests`).
    2. إجراء المراجعة والتدقيق المعماري الصارم (Devil's Advocate Audit).
    3. فحص ومطابقة واعتماد أي كود يتم كتابته من قبل باقي الوكلاء وإصدار قرار الاعتماد النهائي `[APPROVED]`.
* **نموذج OpenCode CLI**: يُعتمد النموذج **Meta Muse Spark 1.3** (`opencode/muse-spark-1.3-contributor-free`) كنموذج افتراضي وحصري لـ `OpenCode CLI` لتنفيذ الأوامر الطرفية، بناء وترحيل قواعد البيانات، وعملاء الاتصال والبيانات (كتابة وبناء دون تشغيل الاختبارات).
* **توزيع المسؤوليات والحدود**:
  * `Claude Code CLI` (النموذج: `Claude 5 Opus / Opus Max`): المراجعات المعمارية، التدقيق المعماري الصارم، **التنفيذ الحصري لكافة الاختبارات**، وبوابات الجودة (Quality Gates).
  * `OpenCode CLI` (النموذج: `Meta Muse Spark 1.3`): **التفعيل الإلزامي للمهام الطرفية وقواعد البيانات** — تنفيذ الأوامر الطرفية، بناء وترحيل قواعد البيانات، كتابة الـ DAOs والـ Repositories والبويلربليت لترشيد استهلاك الكوتا في النماذج العليا (كتابة وبناء دون تشغيل الاختبارات).
  * `Antigravity CLI` (`agcli` - النموذج: `Gemini 3.8 Flash High`): المنفذ الطرفي الذاتي لبناء الواجهات، عقود ومنطق الـ Domain، وتوليد الكود برمجياً (كتابة دون اختبار).
  * `Antigravity IDE` (النموذج: `Gemini 3.8 Flash High`): حاضنة المطور التفاعلية ومقر التحكم البصري (Workbench & Cockpit)؛ ولا تُوجّه إليها أوامر تفويض الـ CLI البرمجية لمنع الحاجة للنسخ واللصق اليدوي.
* **التفويض والتشغيل المباشر الذاتي (Autonomous Direct Dispatch) [CRITICAL RULE]**:
  * **يُحظر تماماً مطالبة المستخدم بنسخ ولصق أوامر أو برومبتات يدوياً لنوافذ الـ CLIs الأخرى.**
  * يقوم الوكيل المنسق بتشغيل وتفويض الأوامر مباشرة عبر أدوات الطرفية (`run_command` أو `manage_task` send_input أو عبر `claude -p` / `opencode run` / `agcli run`).
* **بروتوكول التراجع الذاتي لقيادة الأسطول عند نفاد كوتا Claude Code CLI (Fleet Quota Limit Failover)**:
  * عند بلوغ `Claude Code CLI` حد استهلاك الجلسة (`Session Quota Exceeded / Limit Hit`)، يُحظر تماماً إيقاف المهام أو تعليق خط الإنتاج.
  * يتولى وكيل `Antigravity IDE` (النموذج: `Gemini 3.8 Flash High`) فوراً وبشكل ذاتي قيادة الأسطول وإجراء كافة التعديلات البرمجية وتحديث الجداول والاعتماد الهندسي دون أي توقف، مع استمرار تفويض المهام الطرفية الروتينية لـ `OpenCode CLI`. وعند تجدد رصيد وجلسة Claude، تعود مهام المراجعة المعمارية الحصرية والاختبارات إليه تلقائياً.

---

## 14. معيار معالجة المسافات في مسار Flutter SDK (SDK Space Junction Standard)

* عند وجود مسافات في مسار تثبيت Flutter SDK (تسبب كسر `native-assets` مع حزم C/SQLite مثل `sqlite3` و `drift`)، يتم اعتماد الروابط الوصلية (`Directory Junctions` عبر `cmd /c mklink /J "C:\flutter_sdk" "<Flutter_Path>"`) وتنفيذ أوامر Flutter حصرياً عبر هذا المسار الآمن.

## 15. بروتوكول ترحيل مخططات PocketBase والسكربتات غير التفاعلية (PocketBase & Scripting Standards)

* **ترتيب تبعيات الحذف**: عند ترحيل المخططات السحابية في PocketBase، تُحذف المجموعات القديمة المتعارضة بترتيب عكسي للتبعيات (الأبناء أولاً: daily_logs ➔ weekly_reports ➔ student ➔ students ➔ teachers ➔ circles ➔ academies) لمنع أخطاء قيود التحويل (Auth to Base Immutability).
* **إلزامية السكربتات غير التفاعلية**: يجب أن تقبل كافة سكربتات الأتمتة التوكنات وكلمات المرور كمعاملات (Arguments) أو متغيرات بيئة (Env Vars) وتجنب دوال TTY المعطلة في الخلفية مثل `getpass`.

---

## 16. بروتوكول التشغيل التلقائي الإلزامي لخطاف التدقيق وحراسة الجودة (Hook 19 Post-Edit Automation)

* **الأتمتة بعد كل تعديل:** يُلزم الوكيل الرئيسي وفريق التطوير بتفعيل خطاف 19 تلقائياً فور الانتهاء من أي تعديل برمجي (سواء كان إصلاح خلل أو بناء ميزة) دون انتظار طلب مباشر من المستخدم.
* **إجراءات الخطاف الإلزامية:**
  1. **التحقق من الكود النظيف (`clean-code-guard`):** تطبيق مبادئ DRY/KISS والتأكد من خلو الكود من التكرار أو التجاوزات المعمارية.
  2. **حراسة الاختبارات (`test-guard`):** التأكد من تغطية الحالات الحدية وتفويض الاختبارات لـ Claude Code فقط.
  3. **حراسة التوثيق (`docs-guard`):** توثيق التغييرات وضمان اتساق الذاكرة وسجلات النظام.
  4. **التشغيل والتثبيت على الهاتف (`devops-deployer`):** تشغيل أو تثبيت الإصدار الأحدث تلقائياً على الهاتف المتصل عبر ADB بعد كل تعديل.
## 17. معيار الأرشفة والإدراج الآلي لمكتبة الأوامر (Prompt Library Curator Standard - Hook 26)

* **الالتقاط والهندسة التلقائية:** بمجرد ورود فكرة جديدة أو أمر نوعي أو تفعيل محفزات الخطاف 26، يتولى الوكيل صياغة الموجه بثلاثية معيارية (الاسم المعبر والإيموجي، الأمر البرمجي الصريح، والوصف الدقيق).
* **الإدراج الآلي المحصن (`add-prompt`):** إدراج البرومبت برمجياً في مصنف الإكسيل المركزي `مكتبة الأوامر.xlsx` مع التوجيه الذكي للتبويب المناسب، وتطبيق التنسيق الصارم (خط Segoe UI، خط Consolas للأكواد، والحدود الرفيعة)، مع الاعتماد التلقائي على Excel COM Automation في حال كان الملف قيد الفتح لتفادي أخطاء قفل الملفات.
* **الإبلاغ الإلزامي بالموقع:** يُلزم الوكيل بطباعة اسم التبويب ورقم الصف الدقيق (`Row #X`) فور إتمام الإضافة لتسهيل وصول المستخدم إليه.

## 18. بروتوكول حوكمة وإدارة نافذة السياق ومستشار القرارات (Context Window Governance - Hook 27)

* **المراقبة والاستعلام اللحظي:** فحص استهلاك السياق في محرر Antigravity بانتظام باستخدام أداة `ag-context` و `context-advisor` (أو `ag-context -a`) لقراءة التليمتري الحقيقية من قاعدة بيانات SQLite WAL.
* **معيار المعايرة على النطاق الذهبي للإنتاجية القصوى (30,000 إلى 300,000 توكن):**
  يُحظر تماماً قياس أمان السياق استناداً إلى سعة العتاد الصلبة (1M/2M)؛ لأن الانتباه والتركيز الفائق (Peak Attention Fidelity) ينحصر هندسياً وتجريبياً في النطاق الذهبي (30k إلى 300k توكن).
* **الالتزام الصارم بمناطق الأمان الأربع للنطاق الذهبي (300k Sweet Spot):**
  1. **المنطقة الخضراء (< 180k توكن | أقل من 60% من النطاق الذهبي):** أمان وانتباه تام ودقة 100%؛ الاستمرار في التطوير دون أي تدخل مع ترشيد القراءة عبر `lean-ctx` (Signatures).
  2. **المنطقة الصفراء (180k - 240k توكن | 60% - 80%):** تنبيه مبكر واقتراب من حافة النطاق الذهبي؛ حظر قراءة الملفات الكاملة (`mode='full'`) والاعتماد على فحص أجزاء الأسطر لتفادي الخروج من النطاق الأخضر.
  3. **المنطقة البرتقالية (240k - 300k توكن | 80% - 100%):** منطقة التخطيط للتسليم والضغط (Pre-Handoff Planning)؛ إنهاء المهمة الجزئية الحالية فوراً، تثبيت الكود بعمل `git commit` نظيف، حفظ الدروس في `MEMORY_STORE.md`، والتأهب للضغط أو التسليم.
  4. **المنطقة الحمراء (> 300k توكن | تجاوز سقف النطاق الذهبي):** خروج صريح من نطاق الدقة القصوى؛ حظر بدء مهام جديدة، والتفعيل الإلزامي الفوري للخطاف 25 (التصفير الاستراتيجي وتسليم الجلسة) وتوليد برومبت الاستئناف ثم تنفيذ `/clear` للعودة فوراً للنطاق الأخضر.
* **الاستجابة الفورية:** عند كتابة "مستشار السياق" أو "إدارة السياق" أو أمر `context-advisor`، يلتزم الوكيل فوراً بعرض لوحة التشخيص وتحديد موضع الجلسة من النطاق الذهبي (300k) وتقديم القرار الهندسي التالي جاهزاً للنسخ.

</div>

