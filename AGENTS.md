# دليل القواعد العامة لبيئة هندسة التخطيط — مؤسسة إعمار الفرعة

## 1. التوجيهات اللغوية

- **اللغة العربية الفصحى إلزامية ومطلقة:** في كافة المخرجات، الردود، التوضيحات، والمستندات. يُحظر تماماً كتابة عناوين رئيسية أو فرعية باللغة الإنجليزية أو استخدام تعبيرات إنجليزية مجردة وسط النصوص.
- **تنسيق RTL الشامل:** إلزامية تغليف جميع الردود والتقارير وكافة ملفات Markdown (مثل walkthrough.md و implementation_plan.md و learning_proposal.md) بـ `<div dir="rtl">`، وضبط الاتجاه من اليمين لليسار في ملفات DOCX و PDF.
- **المصطلحات الفنية الهندسية:** تُذكر دائماً بصياغتها العربية المعتمدة أولاً، ويُسمح فقط بإدراج المقابل الإنجليزي بين قوسين `(...)` عند أول ذكر إذا تطلبت الدقة الهندسية ذلك، مع منع اللغات الهجينة في صلب النص.
- **انضباط منظومة الأرقام (معيار الأرقام الإنجليزية المعتمد):**
  - **الأرقام الإنجليزية (0-9):** تُعتمد حصرياً وبشكل مطلق في كافة المستندات الرسمية، عروض الأسعار، جداول الكميات (BOQ)، الخطابات التنفيذية، وثائق المنهجية، ملفات Excel (XLSX)، ملفات Word (DOCX)، وملفات PDF، وكافة التقارير الهندسية المعروضة للمستخدم.
  - **حظر الأرقام الهندية المشرقية (٠-٩):** بناءً على الميزة المعتمدة للمشروع، يُحظر استخدام الأرقام الهندية المشرقية منعاً لتشتت التنسيقات وتوحيداً للأرقام الإنجليزية.
- **بوابة الفحص الآلي الصارمة للأرقام (Automated Zero-Indic-Digits Assertion Gate):** في كافة المستندات الرسمية الصادرة بصيغة PDF و DOCX و XLSX، يُلزم الوكيل برمجياً بفحص النص المستخرج عبر `PyMuPDF` والتحقق من أن عدد الأرقام الهندية المشرقية `len(re.findall(r'[\u0660-\u0669]', text)) == 0` (صفر تماماً)، مع التحقق الصارم من الحفاظ على كافة التنسيقات الهندسية، محاذاة الجداول BiDi/RTL، وقيد الصفحة الواحدة (Strict 1-Page A4).
- **صياغة التواريخ النصية الصريحة (No-Slash Date Standard):** لمنع انقلاب الشرطات المائلة في محركات العرض ثنائية الاتجاه (BiDi)، تُكتب التواريخ في المستندات والخطابات بصيغة النص العربي الصريح (مثال: `٠٦ سبتمبر ٢٠٢٦م` أو `٢٤ صفر ١٤٤٨هـ`) ويُمنع استخدام الفواصل المائلة المجردة.
- **بوابة الفحص الذاتي (Pre-Delivery Gate):** يتولى محرك التحسين الذاتي (self-refinement-engine) تدقيق المخرجات لغوياً وضمان استيفاء معايير القاعدة رقم 1 بنسبة 100% قبل العرض النهائي.

## 2. معلومات المؤسسة الثابتة

```
اسم المؤسسة: مؤسسة إعمار الفرعة للمقاولات العامة
النشاط: مقاولات عامة (إنشاء، ترميم، تشغيل وصيانة)
المقر والعنوان الرسمي: الرياض (يُحظر تماماً ذكر "شارع الفرعة العام" أو "حوطة بني تميم" في بطاقات العنوان الرسمية للمؤسسة)
السجل التجاري: 1013000965  |  عضوية الغرفة التجارية: 151722
جوال إدارة المشاريع: 0546695810 — 0504246410 — 0554460818 (أرقام إنجليزية 0-9 حصرياً)
البريد الإلكتروني المعتمد: emaaralfharah2040@gmail.com  |  engmohamedyones@gmail.com
الذراع الصناعي التابع: مصنع منصة الإبداع للمعادن (مدينة الرياض — نطاق المصانع حي المشاعل — شارع مرج راهط | ترخيص صناعي: 4703229116 | تصريح بيئي: EPOPP-2025-001934)
```

> ملاحظة: بيانات السجل التجاري والعنوان التفصيلي ورقم التواصل تُستكمل من المستخدم

## 3. تصنيف المشاريع

| النوع | الوصف | الوكيل الرئيسي |
| :--- | :--- | :--- |
| إنشاء وترميم | مشاريع بناء جديدة أو إعادة تأهيل | `schedule-builder` + `methodology-writer` |
| تشغيل وصيانة | عقود صيانة دورية وتشغيل مرافق | `schedule-builder` + `methodology-writer` |
| توريدات | توريد مواد ومعدات | `schedule-builder` + `boq-analyst` |
| استشارات | دراسات وإشراف هندسي | `methodology-writer` |

## 4. نظام المراجع

- **الفهرس المركزي:** `.agents/skills/references/index.md`
- **دليل خطوات الاستلام الابتدائي:** [HANDOVER_STEPS_GUIDE.md](file:///G:/M.Yonis/.agents/HANDOVER_STEPS_GUIDE.md) (خطوات عملية وتقنية لتعديل مستندات الاستلام للمشاريع)
- **قبل إنتاج أي مستند:** يقرأ الوكيل الفهرس ويجد المرجع المناسب
- **البطاقة المرجعية:** تحدد الهيكل والتنسيق الملزم
- **إذا لم يوجد مرجع:** يسأل المستخدم أو يستخدم الهيكل الافتراضي

## 5. معايير جودة المخرجات

### ملفات Excel (XLSX)

- ترويسة واضحة بألوان موحدة
- الصف الأول: عناوين الأعمدة (Bold + Background Color)
- تنسيق الأرقام: فواصل الآلاف + خانتان عشريتان للمبالغ
- حدود الخلايا (Borders) على جميع البيانات
- اتجاه الورقة RTL
- **استبدال شعارات المقاول المدمجة برمجياً:** عند وراثة قوالب إكسيل رسمية من جهات الإشراف (TBC / الاستشاري)، يجب فحص حزمة الوسائط (`xl/media/`) واستبدال أي شعارات قديمة للمقاول السابق داخل حاوية الـ Zip بشعار مؤسسة إعمار الفرعة المعتمد (`EFC_LOGO_Excel_Fit.jpg`) مع مطابقة نسبة الأبعاد الهندسية (Aspect Ratio) لإطار الرسم لمنع التشوه البصري.

### ملفات Word (DOCX)

- خط: Traditional Arabic أو Sakkal Majalla
- **المنظومة الخماسية لضبط اتجاه RTL وتفادي مفارقة BiDi (OpenXML BiDi Paradox):**
  1. **تفعيل اتجاه المقطع:** حقن عنصر `<w:bidi/>` في خصائص كل مقطع `<w:sectPr>`.
  2. **المحاذاة النصية للفقرات:** استخدام المحاذاة الموزونة `<w:jc w:val="both"/>` أو لليسار البرمجية `<w:jc w:val="left"/>` داخل `<w:pPr>` مع وجود `<w:bidi/>`؛ حيث أن استخدام `right` في بيئة BiDi يدفع النص بصرياً لليسار (`X0 ≈ 83.5 pt`).
  3. **اتجاه المقاطع النصية:** حقن `<w:rPr><w:rtl/></w:rPr>` في كافة كائنات Runs لمنع انقلاب المصطلحات الإنجليزية والأرقام والأقواس.
  4. **اتجاه الجداول:** حقن `<w:bidiVisual/>` داخل `<w:tblPr>` لكافة الجداول المنشأة في مستندات Word لضمان انطلاق العمود الأول (م) من أقصى اليمين البصري وتفادي انقلاب الأعمدة عند التصدير لـ PDF.
  5. **علامة RLM الخفية (`\u200F`):** إدراج الرمز في بدايات الفقرات والنقاط التي تستهل بمصطلحات لاتينية أو أكواد فنية لفرض انطلاق مؤشر العرض من اليمين.
- **فحص إحداثيات الانطلاق عبر PyMuPDF:** التأكد برمجياً من ارتكاز الكتل النصية على اليمين البصري (`X1 >= 520 pt` على ورق A4).
- **تطهير الجداول من الامتدادات والمسميات الخام:** يُحظر إدراج أسماء الملفات البرمجية بامتداداتها (`.xlsx`, `.pdf`) أو مصطلحات إنجليزية مجردة داخل خلايا الجداول الرسمية، وتُستبدل بالمسميات والتعاريف الهندسية العربية المعتمدة.
- الورق الرسمي: استخدام الورق الرسمي المعتمد (سواء للمقاول الرئيسي أو المورد) كخلفية (Page Underlay) كاملة في PDF مع قياس ارتفاع الترويسة المطبوعة بدقة وضبط الهامش العلوي لمستند Word (Top: 1.22" للمقاول، 2.36" للمورد، Bottom: 0.85") لمنع أي تداخل بين النصوص والترويسة المطبوعة دون إنشاء ترويسات نصية مكررة.
- تظليل الجداول: تظليل موحد أبيض نقي (`#FFFFFF`) لكافة الجداول والبنود ومنع التظليلات الملونة أو الأخضر الداكن في بنود الضمان.
- هوامش ومحاذاة: متناسبة ومضغوطة لضمان قيد الصفحة الواحدة (Strict 1-Page A4) والتحقق البرمجي التلقائي عبر PyMuPDF من عدم تداخل النصوص مع الترويسة أو الأختام.

### ملفات PDF

- مطابقة تامة لمصدر DOCX/XLSX
- خطوط مدمجة (Embedded Fonts) لضمان العرض الصحيح
- حجم الملف محسّن
- **عزل الأصول متعددة اللغات (Multi-Language PDF Asset Isolation):** عند إنتاج أو تحديث نسخ بلغات أخرى (مثل النسخة الإنجليزية للكتالوجات أو البروفايلات الفنية):
  * يُمنع منعاً باتاً تعديل أو الكتابة فوق النسخ اللغوية الأخرى المعتمدة نهائياً (مثل النسخة العربية المعتمدة).
  * الاعتماد الحصري على النسخة الاحتياطية النظيفة (`_backup.pdf`) في التوليد النقي أحادي الطبقة (Single-Pass Lossless Overlay).
  * تخصيص أسماء ملفات مستقلة وواضحة للإصدارات الجديدة وفحص ثبات البصمة الزمنية والحجم للملفات المحمية.

### نماذج طلبات عروض الأسعار والتوريد (RFQ)

- **حظر نقل أرقام وأكواد أصناف الموردين السابقين:** عند إعداد أو استخراج طلبات عروض الأسعار ونماذج التوريد استناداً إلى عروض أسعار موردين سابقين، يُمنع منعاً باتاً إدراج أرقام أو أكواد الأصناف (Item Codes) الخاصة بأنظمة الموردين القدامى، والاكتفاء الحصري ببيان المادة الفني التفصيلي، والمقاسات، والمواصفات الهندسية، والكمية، والوحدة، للحفاظ على السرية وتفادي إرباك الموردين الجدد بأكواد أنظمة لا تخصهم.

### عروض أسعار ومقايسات المقاول المنفذ غير المسعرة (Strict Zero-Pricing & Contractor Proposal Standard)

- **تثبيت هوية المقاول المنفذ:** مؤسسة إعمار الفرعة للمقاولات العامة هي "المقاول العام المنفذ لأعمال التوريد والتركيب" مباشرة لصالح العميل / الجهة المالكة، وليست جهة استدراج عروض أسعار من موردين أو مصانع.
- **الحظر الصارم لوضع الأسعار التقديرية (Strict Zero-Pricing Enforcement):** يُمنع منعاً باتاً وبشكل قاطع وضع، افتراض، أو تخمين أي أسعار للمتر المربع، أو احتساب مبالغ إجمالية أو ضرائب ما لم يقدمها المستخدم صراحة بنص مباشر. يجب تجهيز كافة الجداول والمقايسات بحقول مفرغة تماماً:
  * **في ملفات Excel (XLSX):** ترك خلايا أسعار الوحدات فارغة (`None`)، وتضمين خلايا الإجماليات والضرائب معادلات شرطية ذكية مثل `=IF(K9="","",I9*K9)` و `=IF(L29="","",L29*0.15)` لتعمل ديناميكياً فور كتابة السعر يدوياً دون إظهار أصفار أو أخطاء.
  * **في ملفات Word و PDF:** تُعرض خانات الأسعار والإجماليات كأقواس مفرغة ونظيفة ومجهزة للكتابة اليدوية أو الاعتماد المالي: `[             ر.س ]` مع نقاط للتفقيط `........................................`.
- **الحصر المعماري التفصيلي:** استخراج كامل النماذج والأبعاد والمسطحات وتوصيف الضلف المتحركة والثابتة من المخططات، مع بيان مواصفات قطاعات الألمنيوم، كود الدهان (مثل RAL 7039)، والزجاج المعزول واشتراطات الكود السعودي للبناء.
- **الحزمة الثلاثية المتكاملة:** تصدير المخرجات دائماً بالصيغ الثلاث (XLSX, DOCX, PDF) مع الحفاظ التام على الورق الرسمي لمؤسسة إعمار الفرعة، قيد الصفحة الواحدة الصارم (Strict 1-Page A4)، وبوابة الصفر أرقام هندية مشرقية في ملفات الـ PDF واعتماد الأرقام الإنجليزية (0-9) حصرياً.

### لوحات الفحص التفاعلية ومرفقات المستخلص الختامي (Interactive Handover & Auto-Hydration Standard)

- **أتمتة إدراج مرفقات الصرف الـ ٧ (Automated Invoice Attachments Injection):** في حزم تقديم المستخلصات الختامية (مشاريع TBC والجهات الحكومية)، يُحظر دمج مرفقات الصرف في بند واحد أو مطالبة المستخدم بإدخالها يدوياً. يجب برمجياً إدراج مرفقات الصرف الـ ٧ (خطاب التقديم، ملخص الأعمال، سجل الدفعات Ledger، نموذجي طلب الصرف، شهادة الدفع Payment Certificate، وملخص التقييم المالي Valuation Summary) كبنود تتبع مستقلة (`inv_1` إلى `inv_7`) تحت قسم مستندات المشروع.
- **الترطيب التلقائي للواجهة (Zero-Touch Auto-Hydration):** يجب حقن كائن فحص المجلدات المخبوز (`efc-baked-audit-data`) داخل صفحة HTML وتشغيل دالة الترطيب الذاتي `hydrateFromBakedAudit()` فور تحميل الصفحة لمزامنة خانات الاختيار، شريط التقدم، ونسبة الجاهزية، وشارة الزر العائم (FAB Badge) لحظياً دون انتظار تدخل أو نقر يدوي من المستخدم.
- **الربط المعماري التفاعلي:** ربط كافة مرفقات الصرف الـ ٧ بالعقدة المعمارية المقابلة (`final_invoice`) في مخططات Archify لإبرازها بصرياً (Hover Glow) عند تفاعل المستخدم.
- **بوابة فحص الأرقام الصارمة (Rule 1 Zero-Indic-Digits Gate):** التحقق البرمجي التلقائي عبر التعبيرات النمطية من انعدام الأرقام الهندية المشرقية تماماً (`re.findall(r'[\u0660-\u0669]', text) == 0`) في تقارير التدقيق والمطابقة والمستندات الصادرة واعتماد الأرقام الإنجليزية (0-9) حصرياً.

### تخصيص خطابات ومستندات المستخلصات الموروثة (Inherited Handover Template Standard)

- **حماية القوالب بالأرشفة:** عند وراثة أو استلام قالب DOCX رسمي لخطاب تقديم مستخلص أو نموذج استلام من مشاريع أو مقاولين سابقين، يُلزم النظام بأخذ نسخة احتياطية نظيفة فوراً باسم `_template_backup.docx` قبل إجراء أي تعديل.
- **الحقن الحصري من المعتمد:** تُستخرج بيانات المشروع (اسم المدرسة، رقم المشروع، رقم أمر الشراء PO، المبالغ قبل وبعد الضريبة، والتفقيط الدقيق) حصرياً من شيت الإكسيل المعتمد رسميّاً (`_Approved...xlsx`)، مع استبدال اسم المقاول السابق بـ "مؤسسة إعمار الفرعة للمقاولات العامة".
- **قيد الصفحة الواحدة الصارم (Strict 1-Page A4):** يتم التعديل على مستوى كائنات الـ Runs للحفاظ على أوزان الخطوط والمسافات والتباعد، مع التحقق البرمجي التلقائي عبر PyMuPDF من أن عدد صفحات الـ PDF الناتج = ١ تماماً.
- **الإيداع التلقائي في حزمة التسليم:** تُنسخ المستندات الناتجة (DOCX و PDF) آلياً إلى تبويب `01_المستخلص_المعتمد` بحزمة تقديم المستخلص الختامي.

### معايير ترويسة وهوية البروفايلات والوثائق الموحدة (Corporate Header & Profile Standard)

- **الترويسة العلوية المعتمدة (`header_emaar.png`):** في البروفايلات، الكتالوجات، والوثائق التعريفية، يُعتمد بشكل إلزامي استخدام البانر الرأسي للترويسة الرسمية المعتمدة (`header_emaar.png`) في أعلى الصفحات والفواصل والغلاف الختامي بدلاً من الشعار المستطيل المصغر، مع وضعها داخل حاوية نقية مدعومة بخط فاصل ملكي لمنع أي تشوه بصري.
- **اقتصار المقر على الرياض:** يُذكر عنوان ومقر المؤسسة حصرياً بصيغة **«الرياض»**، مع الحظر الصارم لذكر «شارع الفرعة العام» أو «حوطة بني تميم» في بيانات المؤسسة، وحذف أرقام الهواتف الأرضية مع إدراج البريدين الرسميين المعتمدين (`emaaralfharah2040@gmail.com` و `engmohamedyones@gmail.com`) وأرقام الجوالات بالأرقام الإنجليزية (0-9).
- **الخط الطباعي الملكي المعتمد (Amiri Typography):** يُعتمد خط **الأميري (Amiri-Bold و Amiri-Regular)** في تصميم وتحديث أغلفة البروفايلات، الصفحات الفاصلة، البطاقات التعريفية، وعناوين المشاريع الرسمية لضمان الهوية البصرية الفخمة وتفادي خطوط النظام الافتراضية.

## 6. الاشتراطات الحكومية العامة

- **وزارة البلديات والإسكان:** الالتزام بنماذج العقود والمواصفات العامة للأعمال البلدية
- **جامعة الأمير سطام بن عبدالعزيز:** الالتزام بمعايير التأهيل والتقييم الفني الخاصة بالجامعة
- **صندوق الشهداء:** الالتزام بشروط عقود التشغيل والصيانة
- **ضريبة القيمة المضافة:** 15% على جميع المبالغ
- **التأمينات:** 5% ضمان ابتدائي، 10% ضمان نهائي (حسب العقد)

## 7. منهجية خط الإنتاج

عند استلام طلب مشروع جديد:

1. **`planning-engineer`** يحلل الطلب ويصنّف المشروع
2. **`schedule-builder`** يبني WBS والجدول الزمني
3. **`methodology-writer`** يكتب المنهجية
4. **`boq-analyst`** يحلل الكميات والأسعار (إذا لزم)
5. **`risk-planner`** يعد خطة المخاطر (إذا لزم)
6. **`saudi-code-validator`** يراجع المطابقة
7. **`document-formatter`** يُخرج الصيغة النهائية
8. **`windows-file-organizer`** + **`windows-c-drive-optimizer`** يتعاونان لتنظيم ملفات الجهاز مع حماية روابط العبور (Junction Links) النشطة للقرص C.
9. **`self-refinement-engine`** يراجع المخرجات ذاتياً بناءً على قواعد `04_Self_Refinement_Engine` لضمان تغليف RTL، مطابقة الأرقام، وعدم تجاوز الصفحة الواحدة قبل العرض النهائي.

## 8. محرك التحسين والمراجعة الذاتية (Self-Refinement Strategy)

يتم تفعيل استراتيجية **حلقة التحقق المغلقة (Closed-Loop Validation)** تلقائياً في نهاية كل عملية توليد:

- **تغليف الـ RTL:** التأكد التلقائي من تغليف جميع النصوص والردود بـ `<div dir="rtl">`.
- **فحص مستندات Word:** التحقق الآلي من عدد صفحات الـ DOCX والتأكد من عدم مجاوزة الصفحة الواحدة للخطابات الرسمية.
- **مطابقة الأرقام:** اعتماد الأرقام الإنجليزية (0-9) حصرياً لكافة المستندات والخطابات والجداول والخلايا والأكواد مع حظر الأرقام الهندية المشرقية تماماً.

## 9. سجل التعلم والتحسين

للحفاظ على نظافة وهيكلة هذا الملف، تم فصل "سجل التعلم والتحسين" (الدروس المستفادة، القواعد المكتسبة، والمشاكل التقنية وحلولها) في ملف مستقل.

- **سجل الذاكرة المركزي:** [MEMORY_STORE.md](file:///g:/M.Yonis/.agents/MEMORY_STORE.md)

> يتم تحديث هذا السجل التابع تلقائياً من قِبل وكلاء النظام (persistent-memory-engine) لتجنب تكرار الأخطاء وتسريع عملية الإنتاج.
